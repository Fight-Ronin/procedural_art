{
  "name": "procedural-art",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "tsc --noEmit",
    "preview": "vite preview",
    "test": "node --experimental-strip-types test/run.ts",
    "verify": "npm run check && npm test",
    "render": "node --experimental-strip-types tools/render.ts",
    "ref": "node --experimental-strip-types tools/refs.ts",
    "video": "node --experimental-strip-types tools/video.ts",
    "thumbs": "node --experimental-strip-types tools/thumbs.ts"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "playwright-core": "^1.63.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
